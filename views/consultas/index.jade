doctype html
html(lang="en")
  head
    title= pageTitle
    link(link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css" rel="stylesheet" type="text/css")
    script(type='text/javascript').
      if (foo) {
         bar(1 + 5)
      }
  body
    h1 Buscar Cliente
    form(method="post" action="/busqueda" style="margin-top:1.5em;")
        div(class="form-group left-block col-md-4 ")
          label(style="width: 150px;") Nombre
          div(class="form-group text-left")
          input(type="text" name="name" placeholder="Nombre del cliente" class="form-control")
          input(type="submit" class="btn btn-info" value="Buscar")
    table(class="table")
      caption Clientes
      thead
        tr
          th Nombre
          th Correo
          th Comida
          th Música
          th hobbies
      tbody
        each customer in customers
          tr
            td=customer.name
            td=customer.mail
            td=customer.food
            td=customer.musix
            td=customer.hobbies
